<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="GallogForms.Views.HomePage"
             xmlns:vm="clr-namespace:GallogForms.ViewModels"
             Title="{Binding Title}" 
             BackgroundImageSource="Weed.png" Scale="1.4">

    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="logo.png"/>
    </ContentPage.ToolbarItems>

    <ContentPage.BindingContext>
        <vm:HomeViewModel />
    </ContentPage.BindingContext>

    <Grid Padding="0,120,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="280" />
        </Grid.RowDefinitions>
        <Label Text="Welcome to" FontSize="24" TextColor="white" HorizontalOptions="Center" FontFamily="zekton_regular.ttf#Zekton Rg" Grid.Row="0"/>
        <Label Text="Galactic Logistics Mobile" FontSize="20" TextColor="white" HorizontalOptions="Center" FontFamily="zekton_regular.ttf#Zekton Rg" Grid.Row="1"/>
        <Image Source="logo_196.png" VerticalOptions="Center" HeightRequest="90" Grid.Row="2" />
    </Grid>





</ContentPage>
