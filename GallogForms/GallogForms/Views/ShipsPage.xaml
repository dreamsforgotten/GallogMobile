<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="GallogForms.Views.ShipsPage"
             xmlns:local="clr-namespace:GallogForms.Views;assembly=GallogForms"
             xmlns:vm="clr-namespace:GallogForms.ViewModels"
             BackgroundColor="#292e4c"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:ShipsViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#2196F3</Color>
            <Color x:Key="Accent">#96d1ff</Color>
            <Color x:Key="LightTextColor">#999999</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout BackgroundColor="#252629" VerticalOptions="Fill">
        <Grid HorizontalOptions="FillAndExpand" Padding="4,4,4,4">
            <Button Text="Refresh The Ships" Command="{Binding RefreshItemsCommand}" IsEnabled="True" HorizontalOptions="Center" TextColor="White" BackgroundColor="#252629" CornerRadius="10" FontFamily="zekton_regular.ttf#Zekton Rg" IsVisible="True" />
        </Grid>
        
        <ListView ItemsSource="{Binding Items}" RowHeight="500" BackgroundColor="#252629">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Vertical" Padding="0,0,0,0" BackgroundColor="#252629">
                            <Image Source="{Binding img}" HeightRequest="300" WidthRequest="300" HorizontalOptions="Center" />
                            <Label Text="{Binding mfr}" FontFamily="zekton_regular.ttf#Zekton Rg" TextColor="White" FontSize="24" LineBreakMode="TailTruncation" HorizontalOptions="Center" />
                            <Label Text="{Binding name}" FontFamily="zekton_regular.ttf#Zekton Rg" TextColor="White" FontSize="24" LineBreakMode="TailTruncation" HorizontalOptions="Center" />
                            <Label Text="{Binding flyable}" FontFamily="zekton_regular.ttf#Zekton Rg" TextColor="red" FontSize="22" LineBreakMode="TailTruncation" HorizontalOptions="Center" />
                            <Label Text="{Binding value}" FontFamily="zekton_regular.ttf#Zekton Rg" TextColor="#bb9924" FontSize="22" LineBreakMode="TailTruncation" HorizontalOptions="CenterAndExpand" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ActivityIndicator IsRunning="{Binding IsBusy}"/>
    </StackLayout>
</ContentPage>